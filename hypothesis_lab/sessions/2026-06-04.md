=== SESSION START [this session] ===
Champion status (from finetune/data/meanrev_log.jsonl):
  2026-05-31: holdout_n=941,  rule_win=0.480, rule_ev=+0.0157  (params range>=.0358 bp>=.475)
  2026-06-01: holdout_n=1360, rule_win=0.417, rule_ev=-0.0047  (params range>=.0315 bp>=.470) ALERT EDGE DEGRADING

Priority (from ACTIVE_CONTEXT): H-001 champion degradation diagnosis (P0 URGENT).

Plan: Verify the degradation premise BEFORE dispatching any hypothesis-generation swarm.
  Reason: a 25-idea swarm would be tested through the SAME pipeline that produced the
  champion. If that pipeline is broken, every new test inherits the defect. Fix the
  instrument first.

=== H-001 DIAGNOSIS (main thread, context-loaded — not a cold subagent) ===

Found, by reading code + log (no model guessing):

DEFECT 1 — Unstable-universe "walk-forward".
  autoloop_meanrev.py re-runs build_momentum_v3.py each cycle, which REBUILDS the holdout
  from token_ohlcv over whatever tokens currently have >=30 candles. The two log entries
  are NOT nested in time: n went 941->1360 because the harvest pulled MORE TOKENS, not
  because a market day passed. entry_champion.json shows the +1.57% was validated on just
  34 tokens. Bigger harvest -> measured EV regresses toward the true (negative) memecoin
  base rate (base_ev -0.98% -> -1.56%). The drift detector fired on a confound.

DEFECT 2 — No permutation/CI gate at promotion.
  CONSTRAINTS.md gate requires perm_p<0.05 and CI95 excluding zero. The champion was
  promoted with neither. LEGACY H-03 later permutation-tested the same rule on the full
  n=1360: perm_p=0.2298 -> FAILS the locked gate. The "+1.57%" was a favorable 941/1360
  slice, never significance-tested.

DEFECT 3 — Reported rule_ev is win-rate-implied, not realized.
  build_momentum_v3 stores only token_outcome_is_winner=bool(net>0); the continuous
  triple-barrier payoff is discarded. meanrev_strategy.ev_per_trade(wr) then re-imputes
  EV = wr*0.20-(1-wr)*0.12-0.018, i.e. assumes every trade hits a hard barrier. Most
  trades exit by TIME (small payoff). So logged EV is an artifact, not realized mean PnL.

Also: protocol/H-001 test plan is unrunnable as written — holdout has no token id and no
timestamp, paths in docs are stale (finetune/data/holdout... is actually under training/,
run_hypothesis.py does not exist).

Confirmation in progress: rebuilding from the DB (keeps timestamps + realized payoffs the
holdout file threw away) to compute REALIZED rule EV, permutation-null perm_p, block-
bootstrap CI95, and an honest within-OOS temporal split. Script: hypothesis_lab/scripts/h001_verify.py

=== H-001 RESOLVED — VERDICT: FAIL (champion invalidated) ===
Reproduced pipeline EXACTLY (wr-implied rule_ev −0.4667% == 06-01 log line). Added the
truth the pipeline never computed:
  REALIZED rule_ev = -0.97%   base_ev = -0.17%   edge_over_base = -0.80%
  perm_p = 0.887   CI95 = [-2.25%, +0.64%]   negative in BOTH OOS halves
  left tail: rule avg loss -11.9% vs base -7.5% (fires on names that slam the stop)
=> The mean-reversion drawdown rule has NO out-of-sample edge. It is anti-selective on
   realized payoff. The "+1.57% champion" was a win-rate-implied measurement artifact.

=== INSTRUMENT FIX (shipped + verified end-to-end) ===
- finetune/pipeline/eval_stats.py  NEW — honest scorer (realized EV + perm_p + CI95 +
  verdict). Self-test: planted edge PASSes (perm_p 0), null FAILs (perm_p 0.47). OK.
- finetune/scripts/build_momentum_v3.py — holdout now retains realized `net`, `entry_ts`,
  `token_mint` (was discarding them). Also fixed a datetime deprecation.
- finetune/pipeline/autoloop_meanrev.py — rule_ev is now REALIZED; perm/CI gate added;
  drift only flagged within same `universe_fp` (kills the false-degradation alert);
  added `--validate-only` (DB-only, no harvest).
- Verified: `py build_momentum_v3.py` then `py -m finetune.pipeline.autoloop_meanrev
  --validate-only` -> "rule_ev=-0.97% ... perm_p=0.887 ... NOT-VALIDATED", no false alert.
  Matches h001_verify.py via an independent code path.

=== DOCS UPDATED (honest demotion) ===
H-001 (Results/Verdict/Refinement), champions/STACK.md (empty), C-001 doc (RETIRED),
ACTIVE_CONTEXT.md, INDEX.md, CONSTRAINTS.md (target + 2 anti-patterns), decisions/TREE.md
(D-005 superseded, D-006 locked), ROADMAP.md (re-pointed), entry_champion.json (INVALIDATED,
do_not_trade=true).

=== DELIBERATE DEVIATION FROM PROTOCOL ===
Did NOT dispatch the 6-agent swarm (1 H-001 forensic + 5 generators). Reasons:
  1. H-001 was solvable in the main thread with loaded context in ~10 reads; a cold agent
     would have re-derived everything and hit the stale paths in the protocol/H-001 doc
     (holdout path wrong, no timestamps, run_hypothesis.py absent).
  2. Generating 25 new hypotheses to test through a pipeline that JUST manufactured a false
     champion is the original failure mode. Fix the instrument first. Done.
Next: deepen H-13 (funding carry) + H-15 (anomaly) through eval_stats, OR resume broad
generation — pending user steer (see ACTIVE_CONTEXT "DECISION NEEDED").

Session tally so far: hypotheses tested 1 (H-001 FAIL, decisive), instrument defects fixed 3,
champion invalidated 1, docs re-grounded. Edge found: none yet — search space narrowed correctly.

=== H-15 RESOLVED — VERDICT: REFUTED (was "ANOMALY — investigate") ===
User directive: do all leads in my chosen order, then broad generation. Order: H-15, H-13, H-03 closure, broad-gen.
Ran existing finetune/pipeline/memecoin_neutral.py --analyze (cache: 13 tokens, 57d, numpy 2.4.2 on py3.14).
Reproduced LEGACY exactly: neutral TEST EV +17.59%, t +7.99, perm_p 0.666, CI95 [+7.6%,+31.7%].
The t-vs-perm contradiction RESOLVED via scripts/h015_resolve.py (cluster-robust t-stat):
  naive iid t = +7.99 (assumes 1559 independent events)
  6h-bucket  eff n=54  t=+4.12
  1d-bucket  eff n=14  t=+3.02
  3d-bucket  eff n=6   t=+1.93
  7d-bucket  eff n=2   t=+1.14
  => TEST window spans only 13d ≈ 6 independent SOL-down→bounce episodes. 1559 "events"
     are 4.9× overlapping windows of the same waves across 13 tokens. Effective n is a
     handful, not 1559. t-stat inflated ~3-7×.
  perm_p (20k) = 0.679 — drawdown filter selects NOTHING over random entry.
  regime: 91% of events SOL-down; SOL-up EV NEGATIVE (-4.55%). => long recovery beta, not alpha.
CONCLUSION: identical effective-n pathology as H-001. The "+17.59% anomaly" was 6 lucky
episodes dressed as 1559 iid trades. No edge. memecoin_neutral.py's own verdict() already
gated on perm_p>=0.05 → REFUTED; now formally recorded in INDEX + LEGACY.
NOTE: memecoin_neutral.py is a GOOD reusable engine (panel build, hedge, regime, block-CI,
perm) — keep for future on-chain neutral tests, but its t-stat must be read as overlap-inflated.

=== H-13 RESOLVED — funding carry capturability (scripts/h013_tradeable_carry.py) ===
Offline analysis on cached funding (50 names, 730d, Binance+Bybit+spot), reusing funding_harvest engines.
  [1] single_topk maker FULL universe: TEST apr +9.1% Sharpe 11.4 (reproduces LEGACY +9.0%).
  [4] single_topk RESTRICTED to 29 tradeable names (real spot leg): TEST apr -0.1% REFUTED.
      => the +9.1% is NOT capturable; it times into spot-less RWA/illiquid perps (H, LAB,
         CRCL, MSTR) where long-spot/short-perp is impossible. Inaccessibility premium.
  [4] diversified tradeable single (EW, basis-aware, maker): +0.8% APR Sharpe 1.79 CI[+0.2,+1.3] VALIDATED.
  [5] NOVEL — cross-venue (Binance-Bybit) spread re-tested at MAKER 1bp (LEGACY only did taker):
      taker -9.6% REFUTED -> maker +0.6% APR Sharpe 3.93 CI[+0.0,+1.1] breakeven 1.5bps VALIDATED (fragile).
ANSWER to the task question: the tradeable filter is CORRECT, not too conservative — the carry
lives where you can't run delta-neutral. Capturable carry = two small market-neutral sleeves
(+0.8% robust, +0.6% fragile), stackable to ~+1.4% APR Sharpe ~2-4. Real and POSITIVE but an
order of magnitude below the +5% target. These are the best real edges in the program so far.
META-LESSON: funding_harvest.verdict() VALIDATED the +9.1% — its gate doesn't check that the
dynamically-selected legs are executable. Milder cousin of the H-001/H-15 "metric measures
something you can't capture" theme. Capturability must be a gate, not just stat-significance.

=== BROAD GENERATION + 2 MORE OOS TESTS (H-019, H-020) ===
Generated a focused, filtered batch informed by the session (not cold agents). Tested the best
novel testable-now idea — the CORRECTED H-15 (cross-sectional neutral within memecoins, removes
SOL beta, honest cross-sec permutation, NON-overlapping rebalances):
  H-019 reversion: gross -10.8%/rebal, perm_p 0.90 -> memecoins do NOT revert cross-sectionally.
  H-020 momentum (sign flip): gross +200%/rebal, perm_p 0.003 (rank PREDICTS!) BUT hit 46.7%,
    CI95 [-5.3%,+246%] spans zero -> real signal, LOTTERY payoff. Unsizeable. Same as H-17.
INSIGHT: memecoins TREND not revert; momentum beats its null but is convex/right-tail, so
linear sizing can't harvest it. Explains why C-001 + H-15 + H-019 (all reversion) died.
Filtered survivors queued: H-024 (new-listing funding decay — TOP, novel+structural+testable),
H-021 (persistence-selected carry), H-022 (xvenue agreement filter). Full synthesis in
sessions/2026-06-04-synthesis.md.

=== SESSION CLOSEOUT ===
OOS tested: 5 (H-001, H-15, H-13, H-019, H-020). Passes: 0. Resolved/refuted: 3. Real sub-gate
edges: 2 (carry sleeves +0.8% Sh1.8 / +0.6% Sh3.9, stack ~+1.4% APR). Instrument defects fixed: 3.
Champion: C-001 INVALIDATED — stack empty. New scripts: eval_stats.py, h001_verify.py,
h015_resolve.py, h013_tradeable_carry.py, h019_memecoin_xs_reversion.py. Pipeline fixed:
build_momentum_v3.py, autoloop_meanrev.py.
KEY INSIGHT: the program's "losses" were measurement artifacts, not bad luck. Honest instrument
=> only positive EV in reach is small structural carry; memecoins are convex-momentum vehicles.
NEXT SESSION: H-024 (new-listing funding decay) -> H-021/H-022 (lift carry sleeve to gate).
NOTE FOR USER: nothing committed to git (not requested). All work in working tree + lab docs.

=== SESSION 2 START ===
State: champion stack EMPTY, instrument honest, 3 leads queued.
Mission: H-024 (new-listing funding decay) → H-021 (persistence carry) → H-022 (xvenue agreement) → broad generation (H-031+).
Approach: run the 3 carry tests INLINE via one shared harness (hypothesis_lab/scripts/carry_leads.py)
— they share the funding panel + funding_harvest engine; cold subagents would re-derive it.
Hard constraints (Session 1 truths): no memecoin reversion, no liquid directional, no win-rate-implied
EV, effective-n = distinct independent events. Gate = eval_stats (realized EV + perm_p<0.05 + CI95>0 + n>100).

=== SESSION 2 RESULTS (scripts/carry_leads.py, inline shared harness) ===
H-021 VALIDATED (best edge to date): fixed name-selection beats dynamic chasing.
  ALL-29 EW +0.77% Sh1.79 → TOP-10 by LEVEL (fixed) +1.44% APR Sh3.20 CI[+0.73,+2.05].
  (contrast: H-13 DYNAMIC single_topk −0.1% — turnover/spike-chasing was the killer.)
  STACK level-fixed + xvenue-maker (50/50): +1.02% APR Sharpe 4.28 maxDD −0.1%, corr +0.01.
  => first CHAMPION-CANDIDATE. Unlevered <+2% gate; Sharpe 4.28 levers toward +5% BUT 6mo OOS
     has no crash → maxDD −0.1% understates tail. Leverage gated on longer multi-regime OOS.
H-022 REFUTED: agreement gating makes carry worse at every threshold (agreement = crowded peak).
H-024 FAIL: no systematic new-listing decay (early≈mature, perm_p 0.55-0.99). Hedgeable subset
  hint +20-32% APR but n=8 listings — collect-forward only, n-blocked.

=== GENERATION (12 scored, 10 survivors H-031–H-051) ===
Full batch + scores in sessions/2026-06-04-session2-synthesis.md. Priority files written:
H-031 (risk-parity sizing), H-049 (carry-to-vol selection), H-051 (negative-funding sleeve).
Cut 10 (mostly data-unavailable: options/OI/quarterly/margin-split, or dead families).

=== SESSION 2 CLOSEOUT ===
OOS tested: 3 (1 VALIDATED, 1 REFUTED, 1 FAIL). Champion: candidate logged (carry book Sh4.28),
none promoted. New scripts: carry_leads.py. New hypotheses: H-021/022/024 (results) + H-031/049/051
(files) + 7 more in synthesis. KEY INSIGHT: edge = fixed selection + stacking UNCORRELATED carry
sleeves + leverage (tail-gated), not one big alpha. Path to +5% = more sleeves + honest tail bound.
SESSION 3 #1: H-031 + H-051 (raise the stacked carry book toward the gate). Nothing committed to git.

=== TAIL STRESS (full 730d) — caught a leverage trap ===
Stack over full history: APR +3.16% (full)/+1.02% (OOS), maxDD −0.24%. Naive leverage(budget/maxDD)
implies 8.4x→+27%, 21x→+66% APR. REJECTED as a backtest artifact: maxDD −0.24% is funding-accrual +
8h-close-basis drawdown in a benign window — it does NOT model intra-8h gap/liquidation, basis-blowout
(likely out-of-sample), funding clamp/ADL, or maker-fill failure. At 8-42x one gap = LTCM. Same lesson
as H-001/H-13/H-15: the metric isn't the real risk. Honest: SANE 2-3x → ~+3-6% APR is the route to
+5%, but gated on tick/1m margin simulation + a basis-blowout stress scenario — 8h-close data can't
supply either. REAL Session-3 blocker = DATA (intraday), not more in-sample slicing.

=== SESSION 3 — carry refinements (scripts/carry_lift.py) ===
H-031 risk-parity sizing: +1.49% APR Sh3.54 (vs EW +1.44% Sh3.20). Marginal real lift → default sizing.
H-049 carry-to-vol selection: +1.32% APR Sh3.89 (Sharpe↑ APR↓). Marginal.
H-036 BTC-beta hedge: REFUTED — book beta≈0 already; hedge HURTS (Sh3.20→2.72). Don't hedge.
H-051 neg-funding sleeve: REFUTED — TRAP CAUGHT. Apparent +6.92% APR Sh9.56; 3-stack +3.00% Sh11.97
  (would look like it clears +2% gate). Per-name breakdown: 6/10 selected names had POSITIVE train
  funding; payoff is from test-window funding SIGN-FLIPS (FET train +3.4%→test −23.1%→+23.2% sleeve).
  = non-stationary alt-funding regime capture, effective-n≈1, NOT a predictive edge. Sharpe 12 ⇒ artifact.
  DID NOT promote. A naive read would have minted a false champion C-002.
NET: no promotion. Champion-candidate unchanged: level-fixed (risk-parity-sized) +1.49% APR Sh~3.5,
  +xvenue stack +1.04% Sh4.64. Sub-gate unlevered; leverage-gated on intraday data (not yet harvested).
DECISION: did NOT launch the 21-min 1m-kline harvest this turn (context budget). harvest_intraday_1m.py
  is the prepared Session-4 first step. Honest state: cached-8h analysis is EXHAUSTED for the gate.

=== SESSION 2 START ===
State: champion stack EMPTY, instrument honest, 3 leads queued.
Mission: H-024 → H-021 → H-022 → broad generation.
Dispatched 3 parallel test agents (independent: new-listing carry / persistence-selected carry /
xvenue-agreement carry). Each writes its own script + H-file + heartbeat. I scrutinize results
(effective-n, capturability — guard against false PASS) + own INDEX/STACK/synthesis.
Env facts handed to agents: use `py` launcher (python = Store stub, exit 49); numpy 2.4.2 OK;
offline cached funding-panel loader = hypothesis_lab/scripts/h013_tradeable_carry.py::load_offline_panel.
