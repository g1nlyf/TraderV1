=== SESSION 7 HEARTBEAT — H-166 PAPER RISK/EXIT ENGINE ===
Start 2026-06-06. Operator: Opus 4.8 autonomous. Paper-only. Free data only.

GOAL: convert H-162/H-166 (quality-wallet distribution -> forward drop; exit-overlay saves +3.9/5.4%/trade,
beats shuffled-lag control 100%) into a DETERMINISTIC paper-only Stage-2 risk/exit/no-trade overlay.
Hermes interprets context; deterministic service owns risk/exit.

ENTRY TRUTH: C-002 sole champion. Wallet long alpha DEAD. H-162 real intra-session, NOT cross-day-proven.
H-166 exit-overlay = first capturable wallet behavior (de-risk, not alpha). Firehose built (1 tick so far,
all 2026-06-06 -> cross-day still unresolved; collector must run days).

PLAN (budget-aware, real-progress-first):
- P0 read stage2 risk/exit/paper service shapes (integration target).
- P2/P3 build h166_risk_overlay.py (deterministic API: evaluate_entry->pass/no_trade/watch,
  evaluate_position->hold/exit_candidate, evidence refs, invalidation, rollback) + fixture tests
  (no-leakage, fires-on-quality-sells, not-on-random, exit>shuffled-control, fixtures marked non-organic).
- P4 Stage-2-style backtest vs ALL controls (hold / mechanical-early-exit / random-sell / H-166 exit /
  no-trade veto / combined) -> report.
- P1 firehose hardening (status report, daily rollup, collection-target).
- P6 compound theory: distributor-archetype (point-in-time) + co-sell network, with ablation.
- P7 docs + reports + commit.

LOG:
[t0] heartbeat. Reading stage2 services to match decision/evidence shape before building.

[t1] P0 — read stage2 risk/paper/wallet_intelligence. Contract: RiskService.run_{entry,exit,position_monitoring}
     _risk_check -> risk_check_id + vetoes + evidence + data_as_of (leakage guard). PaperTrading gates exits by
     risk_check. WalletIntelligence exists but uses look-ahead snapshots.
[t2] P2/P3 — h166_risk_overlay.py: deterministic, point-in-time, paper-only. evaluate_entry->pass/watch/no_trade
     (absorbed=watch per H-042 bounce; active=no_trade), evaluate_position->hold/exit_candidate. Degradation
     (no quality -> never hard-veto), source-purity flag, evidence refs. test_h166_overlay.py 8/8 PASS
     (leakage, fires-on-quality, not-on-random, absorbed-watch, exit-trigger, degradation, purity, determinism).
[t3] P4 — backtest_h166.py through the REAL module, ALL controls. KEY RESULT (honest demotion):
     hold -15.08% | exit_random_time -13.92% | exit_random_SELL -11.69% | exit_h166 -12.84% | veto -15.51%(ns).
     exit_h166 beats hold(+2.24)/random-time(+1.08) perm 0.000 BUT LOSES to random-sell(-1.15). veto worthless.
     => quality-distribution specificity adds NOTHING over exit-on-any-sell. Sprint-6 shuffled-lag control was
     too weak; random-sell is the right control. H-166 = research curiosity / one-session artifact, NOT a
     validated Stage-2 module. CVaR5 ~-98% (no tail fix).
[t4] P1 — firehose_status.py (health/rollup/target) + FIREHOSE_STATUS.md. GT free ~0.5 sell-clusters/day <<100
     needed -> free GT too thin; bitquery corecast (free, wired) is the volume path. 2nd tick ran (same day).
[t5] P6 — distributor_theory.py: distributor_score (repeatedly-sells-before-drop) rho +0.262 = BEST wallet
     feature (top-half +25.45% vs base +17.57%); wallet_quality +0.231; cohesion(co-sell network) -0.102 = DEAD;
     token tok_prior_ret +0.332 / tok_cum_sol +0.284 DOMINATE. No wallet/network layer beats token context.
[t6] P7 — reports: H166_RISK_ENGINE / H166_STAGE2_INTEGRATION (shadow-only) / H162_DAY_LEVEL_STATUS /
     WALLET_DISTRIBUTION_THEORY. Knowledge: CANONICAL_STATE override block + INDEX + STACK + DEAD_TRACKS.
     (LIVE_THREADS/ROADMAP essence folded into CANONICAL_STATE override.)
OUTCOME: H-166 deterministic overlay BUILT + TESTED, but BACKTEST DEMOTES IT (loses to exit-on-any-sell; veto
     useless). Distributor archetype = best wallet feature but < token context; network cabal dead. Cross-day
     still UNRESOLVED (1 session; GT free too thin -> corecast path). No promotion. C-002 sole champion.

